<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>IIS Portal • Login</title>

  <!-- Tailwind -->
  <script src="https://cdn.tailwindcss.com"></script>
   <link rel="icon" href="/Images/portal-logo.png">

  <!-- Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@500;700&family=Inter:wght@400;600&display=swap" rel="stylesheet">

  <style>
    body {
      font-family: "Playfair Display", sans-serif;
    }
  </style>
</head>

<body class="bg-slate-50">

  <!-- HEADER -->
  <header class="flex items-center justify-between px-6 py-5 bg-white">
    <a href="/" class="flex items-center gap-3">
      <img src="/Images/portal-logo.png" class="w-16 rounded-full border-2 border-blue-600" />
      <h1 class="text-2xl font-bold text-blue-600">IIS Portal - Admin Staff</h1>
    </a>
  </header>

  <!-- MAIN -->
  <main class="min-h-[calc(100vh-120px)] flex items-center justify-center px-4 py-10">
    <section class="w-full max-w-4xl bg-white rounded-2xl shadow-lg p-8 sm:p-12">

      <h2 class="text-3xl font-bold text-blue-600 mb-2 text-center">Login • Admin Staff</h2>
      <p class="text-gray-600 mb-10 text-center">
        Enter your credentials to access the portal.
      </p>

      <form action="/login/admin" method="POST">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8">

          <!-- LEFT -->
          <div class="flex flex-col gap-6">
              <div>
              <label class="block font-semibold text-blue-600 mb-2 text-lg">
                Phone
              </label>
              <input
                type="tel"
                name="phone"
                required
                placeholder="Enter your phone number"
                class="w-full px-4 py-4 rounded-xl border-2 border-slate-200
                       focus:outline-none focus:border-blue-600
                       focus:ring-4 focus:ring-blue-100 transition"
              />
            </div>
<div>
              <label class="block font-semibold text-blue-600 mb-2 text-lg">
                Secret-Key
              </label>
              <input
                type="text"
                name="secret"
                required
                placeholder="Enter your secret key"
                class="w-full px-4 py-4 rounded-xl border-2 border-slate-200
                       focus:outline-none focus:border-blue-600
                       focus:ring-4 focus:ring-blue-100 transition"
              />
            </div>


            <div>
              <label class="block font-semibold text-blue-600 mb-2 text-lg">
                Password
              </label>
              <input
                type="password"
                name="password"
                required
                placeholder="Your password"
                class="w-full px-4 py-4 rounded-xl border-2 border-slate-200
                       focus:outline-none focus:border-blue-600
                       focus:ring-4 focus:ring-blue-100 transition"
              />
            </div>
          </div>

          <!-- RIGHT -->
          <div class="flex flex-col justify-between">
            <div class="bg-slate-100 rounded-xl p-6 text-gray-700 leading-relaxed">
              <p class="font-semibold mb-2 text-blue-600">Notice</p>
              <p>
                Access level is determined automatically after login.
                Unauthorized accounts will be denied.
              </p>
            </div>

            <button
              type="submit"
              class="mt-6 px-6 py-4 rounded-xl
                     bg-gradient-to-r from-blue-600 to-blue-700
                     text-white font-semibold
                     shadow-md hover:shadow-xl transition"
            >
              Login
            </button>
                 <a href="/create-account/admin">
            <button type="button" class=" w-full mt-4 px-6 py-3 rounded-xl bg-white text-blue-500 font-semibold shadow-md hover:shadow-xl transition" >Register</button>
            </a>
          </div>

        </div>
                <p class="flex justify-center items-center pt-3">Need help?  <a href="/help">-Click here</a></p>
      </form>
    </section>
  </main>

</body>
</html>
