<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>IIS Portal • Teacher Signup</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@500;700&family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: "Playfair Display", sans-serif;
    }
  </style>
</head>
<body class="bg-slate-50">

  <!-- HEADER -->
  <header class="flex flex-col sm:flex-row items-center justify-between gap-4 px-6 py-5 bg-white">
    <a href="/register/teacher" class="flex items-center gap-3">
      <img src="/Images/portal-logo.png" alt="Logo" class="w-16 sm:w-20 rounded-full border-2 border-blue-600"/>
      <h1 class="text-2xl font-bold text-blue-600">Teacher Signup</h1>
    </a>
  </header>

  <!-- MAIN -->
  <main class="min-h-[calc(100vh-120px)] flex items-center px-4 sm:px-6 py-10">
    <section class="w-full max-w-5xl mx-auto text-center">
      <h2 class="text-3xl font-bold text-blue-600 mb-2">Teacher Signup</h2>
      <p class="text-gray-600 mb-10">Fill your details carefully.</p>

      <form action="/createAccount/teacher" method="POST" class="bg-white rounded-2xl shadow-lg p-6 sm:p-10">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8 text-left">

          <!-- LEFT COLUMN -->
          <div class="flex flex-col gap-6">
            <div>
              <label class="block font-semibold text-blue-600 mb-2 text-lg">Full Name</label>
              <input type="text" name="name" required
                     class="w-full px-4 py-4 rounded-xl border-2 border-slate-200 focus:outline-none focus:border-blue-600 focus:ring-4 focus:ring-blue-100 transition"/>
            </div>
            <div>
              <label class="block font-semibold text-blue-600 mb-2 text-lg">Phone Number</label>
              <input type="tel" name="phone" required
                     class="w-full px-4 py-4 rounded-xl border-2 border-slate-200 focus:outline-none focus:border-blue-600 focus:ring-4 focus:ring-blue-100 transition"/>
            </div>
             <div>
              <label class="block font-semibold text-blue-600 mb-2 text-lg">Subject</label>
              <input type="text" name="subject" required
                     class="w-full px-4 py-4 rounded-xl border-2 border-slate-200 focus:outline-none focus:border-blue-600 focus:ring-4 focus:ring-blue-100 transition"/>
            </div>
          </div>

          <!-- RIGHT COLUMN -->
          <div class="flex flex-col gap-6">
            <div>
              <label class="block font-semibold text-blue-600 mb-2 text-lg">Secret code</label>
              <input type="text" name="secret" required
                     class="w-full px-4 py-4 rounded-xl border-2 border-slate-200 focus:outline-none focus:border-blue-600 focus:ring-4 focus:ring-blue-100 transition"/>
            </div>
            <div>
              <label class="block font-semibold text-blue-600 mb-2 text-lg">Password</label>
              <input type="password" name="password" required
                     class="w-full px-4 py-4 rounded-xl border-2 border-slate-200 focus:outline-none focus:border-blue-600 focus:ring-4 focus:ring-blue-100 transition"/>
            </div>

            <button type="submit"
                    class="mt-4 px-6 py-3 rounded-xl bg-gradient-to-r from-blue-600 to-blue-700 text-white font-semibold shadow-md hover:shadow-xl transition">
              Create Account
            </button>
            <a href="/login/teacher">
            <button type="button" class="w-full mt-4 px-6 py-3 rounded-xl bg-white text-blue-500 font-semibold shadow-md hover:shadow-xl transition" >Log-in</button>
            </a>
          </div>

        </div>
      </form>
    </section>
  </main>
</body>
</html>
