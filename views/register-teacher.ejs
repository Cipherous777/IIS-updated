<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>IIS Portal • Register</title>

    <!-- Google Fonts -->
    <link
      href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@500;700&family=Inter:wght@400;600&display=swap"
      rel="stylesheet"
    />

    <!-- Tailwind -->
    <script src="https://cdn.tailwindcss.com"></script>
     <link rel="icon" href="/Images/portal-logo.png">

    <!-- Optional branding -->
    <link rel="stylesheet" href="/CSS/main.css" />
  </head>

  <body class="bg-slate-50">
    <!-- HEADER -->
    <header
      class="flex flex-col sm:flex-row items-center justify-between gap-4 px-6 py-5 bg-white"
    >
      <a href="/register" class="flex items-center gap-3">
        <img
          src="/Images/portal-logo.png"
          alt="IIS Portal Logo"
          class="w-16 sm:w-20 rounded-full border-2 border-blue-600"
        />
        <h1 class="text-2xl font-bold text-blue-600">IIS Portal</h1>
      </a>

      <a href="/portal">
        <button
          class="px-6 py-3 rounded-xl bg-blue-600 text-white font-semibold hover:bg-blue-700 transition"
        >
          Go to portal
        </button>
      </a>
    </header>

    <!-- MAIN -->
    <main class="min-h-[calc(100vh-120px)] flex items-center px-4 sm:px-6 py-10">
      <section class="w-full max-w-5xl mx-auto text-center">
        <h2 class="text-3xl font-bold text-blue-600 mb-2">Register</h2>
        <p class="text-gray-600 mb-10">Fill these forms carefully.</p>

        <!-- FORM -->
        <form
          action="/register/teacher"
          method="POST"
          
          class="bg-white rounded-2xl shadow-lg p-6 sm:p-10"
        >
          <!-- GRID -->
          <div class="grid grid-cols-1 md:grid-cols-2 gap-8 text-left">
            <!-- LEFT COLUMN -->
            <div class="flex flex-col gap-6">
              <div>
                <label class="block font-semibold text-blue-600 mb-2 text-xl">
                  Full name
                </label>
                <input
                  type="text"
                  name="name"
                  placeholder="Full name"
                  required
                  class="w-full px-4 py-4 rounded-xl border-2 border-slate-200
                         focus:outline-none focus:border-blue-600
                         focus:ring-4 focus:ring-blue-100 transition"
                />
              </div>

              <div>
                <label class="block font-semibold text-blue-600 mb-2 text-xl">
                  Phone-no
                </label>
                <input
                  type="tel"
                  name="phone"
                  placeholder="Phone number"
                  required
                  class="w-full px-4 py-4 rounded-xl border-2 border-slate-200
                         focus:outline-none focus:border-blue-600
                         focus:ring-4 focus:ring-blue-100 transition"
                />
              </div>
            </div>

            <!-- RIGHT COLUMN -->
            <div class="flex flex-col gap-6">
              <div>
                <label class="block font-semibold text-blue-600 mb-2 text-xl">
                  E-mail
                </label>
                <input
                  type="email"
                  name="email"
                  placeholder="E-mail"
                  required
                  class="w-full px-4 py-4 rounded-xl border-2 border-slate-200
                         focus:outline-none focus:border-blue-600
                         focus:ring-4 focus:ring-blue-100 transition"
                />
              </div>

              <!-- DESCRIPTION -->
              <div>
                <label class="block font-semibold text-blue-600 mb-2 text-xl">
                  Description
                </label>
                <textarea
                  name="text"
                  rows="1"
                  placeholder="Write a precise description"
                  class="w-full px-4 py-4 rounded-xl border-2 border-slate-200
                         resize-none overflow-hidden
                         focus:outline-none focus:border-blue-600
                         focus:ring-4 focus:ring-blue-100
                         transition-all duration-300
                         h-[56px] focus:h-40"
                ></textarea>
              </div>

              <!-- BUTTONS -->
              <div class="flex flex-col sm:flex-row justify-end gap-4 mt-4">
                <a
                  href="javascript:history.back()"
                  class="text-center px-6 py-3 rounded-xl
                         border-2 border-slate-200 bg-slate-100
                         font-semibold hover:bg-slate-200 transition"
                >
                  Previous
                </a>
                <button
                  type="submit"
                  class="px-6 py-3 rounded-xl
                         bg-gradient-to-r from-blue-600 to-blue-700
                         text-white font-semibold
                         shadow-md hover:shadow-xl transition"
                >
                  Next
                </button>
              </div>
            </div>
          </div>
                  <p class="flex justify-center items-center pt-3">Need help?  <a href="/help">-Click here</a></p>
        </form>
      </section>
    </main>
  </body>
</html>
